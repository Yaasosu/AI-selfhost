global:
  scrape_interval: 5s # Частота сбора метрик (5 секунд для быстрого тестирования)
  evaluation_interval: 5s

scrape_configs:
  - job_name: "fastapi-app"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["backend:8000"]
